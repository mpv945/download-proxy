# 运行完全静态链接的二进制文件（bash/sh都没有）（甚至连 /lib 都没有）
FROM gcr.io/distroless/static
ARG APP_FILE
EXPOSE 8080
COPY ${APP_FILE} /app
ENTRYPOINT ["/app"]

# docker build -f Dockerfiles/Dockerfile \
#             --build-arg APP_FILE=./target/jibber \
#             -t localhost/jibber:native.01 .
